# The CI and development environment, pinned exactly.
#
# WHY EXACT PINS: this branch shipped "mypy --strict clean" while CI was red on
# its first run. Every entry here used to be a floor with no ceiling, so CI
# resolved numpy 2.2.6 / opencv 5.0.0 / torch 2.14.0 against a local machine
# running numpy 1.26.4 / opencv 4.10 / torch 2.4.1. Two real defects followed
# (opencv>=5 type stubs, and torch>=2.6 flipping the torch.load weights_only
# default, which silently disabled the allow_unsafe_load branch).
#
# Ceilings alone would NOT have prevented that: the torch break landed in a
# MINOR release, so `torch<3` would have allowed it through. Only exact pins
# make CI reproducible. The `<major` ceilings in pyproject.toml bound what a
# CONSUMER of the dfd package may install; they are not the drift guard.
#
# SCOPE: direct dependencies only. Transitive dependencies still float, so this
# is reproducible-in-practice, not hermetic. A full transitive lock is the next
# step up if that matters.
#
# TO CHANGE A PIN: bump it here, run all four gates in a venv built from THIS
# file with no local site-packages, and only then commit. tests/test_ci_gates.py
# enforces that every line stays `==`-pinned and stays inside the pyproject range.

pytest==9.1.1
pytest-cov==7.1.0
ruff==0.16.8
mypy==2.3.1
packaging==26.3
numpy==2.2.6
opencv-python-headless==5.0.0.93
pillow==12.3.0
scikit-learn==1.7.2
pyyaml==6.0.3
torch==2.14.0
